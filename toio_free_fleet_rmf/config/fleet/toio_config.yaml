# FLEET CONFIG =================================================================
# Mirrors free_fleet_examples/config/fleet/nav2_unique_multi_tb3_simulation_fleet_config.yaml.
# Per-robot `navigation_stack: 2` selects upstream Nav2RobotAdapter, which is
# what the toio_free_fleet_client speaks (via the zenoh-bridge-ros2dds bridge).

rmf_fleet:
  name: "toio"
  # Kept deliberately conservative (slower than the cube actually is). RMF
  # derives an expected travel time per segment from these and flags the
  # robot "unresponsive" -> replan if it doesn't arrive in time. The toio's
  # motor_control_target (Linear) rotates to face each target before driving,
  # which is slower than a high angular limit implies; under-estimating speed
  # here gives the cube enough time and stops the replan churn.
  limits:
    linear: [0.7, 1.0]    # velocity, acceleration (m/s, m/s^2)
    angular: [1.0, 2.0]   # velocity, acceleration (rad/s, rad/s^2)
  profile:
    # radii in m. The cube body is 31.8 mm square; at the x50 world scale that
    # is a 1.59 m square, so the body radius (inscribed circle, half a side) is
    # 31.8/2 mm * 50 = 0.795 m. (The old 1.25 wrongly used the 1.59 m *side* as
    # a radius, doubling the footprint.) vicinity is footprint + a small
    # clearance, kept below the ~1.2 m minimum waypoint spacing so two cubes can
    # still occupy adjacent waypoints instead of mutually blocking.
    footprint: 0.80
    vicinity:  1.20
  reversible: False  # toio always rotates to face its target then drives forward (motor_control_target Linear); it never reverses
  battery_system:
    voltage: 3.7
    capacity: 0.26
    charging_current: 0.3
  mechanical_system:
    mass: 0.05
    moment_of_inertia: 0.00002
    friction_coefficient: 0.20
  ambient_system:
    power: 0.1
  tool_system:
    power: 0.0
  recharge_threshold: 0.10
  recharge_soc: 0.95
  publish_fleet_state: 10.0  # Hz, keep == robot_state_update_frequency for smooth dashboard updates
  account_for_battery_drain: True
  task_capabilities:
    loop: True
    delivery: True
  finishing_request: "park"
  responsive_wait: True
  # Opt in to the rmf_reservation_node queue_manager (started by
  # common.launch.xml) so parking spot assignment is deconflicted between
  # cubes. Without this flag the fleet adapter ignores the queue manager.
  use_parking_reservations: True
  robots:
    cube_0:
      charger: "toio_0_charger"
      responsive_wait: False
      navigation_stack: 2
      # Bound the blocking get_result zenoh query in Nav2RobotAdapter's
      # _is_navigation_done. Left unset (None) it blocks for zenoh's default
      # (~10 s) whenever the feedback fast-path lapses (e.g. each time a cube
      # reaches a waypoint), freezing the robot's pose updates and the next
      # move command. 1.0 s matches the free_fleet nav2 single-robot example.
      service_call_timeout_sec: 1.0
      init_timeout_sec: 10
      initial_map: "L1"
      map_frame: "map"
      robot_frame: "base_footprint"
      maps:
        L1:
          # TODO: replace with the nav2 map.yaml exported from traffic_editor
          # (see `building_map_generator nav` in the README).
          map_url: ""
    cube_1:
      charger: "toio_1_charger"
      responsive_wait: False
      navigation_stack: 2
      service_call_timeout_sec: 1.0  # see cube_0
      init_timeout_sec: 10
      initial_map: "L1"
      map_frame: "map"
      robot_frame: "base_footprint"
      maps:
        L1:
          map_url: ""

  robot_state_update_frequency: 10.0


# TRANSFORM CONFIG =============================================================
# Keyed by *map level name* (matches `levels:` in toio_map.building.yaml). The 4
# points must be listed in matching order between `rmf` and `robot` — order
# below is top-left, top-right, bottom-left, bottom-right.
#
# `robot` is in the cube's RMF-meter frame (anchored at mat top-left = 0, 0).
# The client publishes TF in this frame already (mat_to_rmf_xy applied on the
# BLE side). These 4 corners describe the physical mat outline: 15.2 × 10.8 m.
#
# `rmf` corners are the same mat outline placed in traffic_editor on the
# toio_map background image. The image has a legend/margin outside the mat so
# the mat corners are inset from the image edges; that small offset is the
# only difference between the two coord systems and is what `nudged` learns.

reference_coordinates:
  L1:
    rmf: [[1.6866,  -1.6016],
          [16.6677, -1.6016],
          [1.6866, -12.043],
          [16.6677, -12.043]]
    robot: [[0.0,   0.0],
            [15.2,  0.0],
            [0.0,  -10.8],
            [15.2, -10.8]]
